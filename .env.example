# Required
DISCORD_TOKEN=your_discord_token
RCON_PASSWORD=your_rcon_password

# Optional - RCON connection (defaults shown)
RCON_HOST=localhost
RCON_PORT=25575

# Optional - Minecraft server container only (not used by bot)
MAX_MEMORY=8G
