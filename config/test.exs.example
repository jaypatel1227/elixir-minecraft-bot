import Config

# Integration tests run against a real Minecraft server with RCON enabled.
# Copy this file to test.exs and set your local server's RCON password.
config :rcon,
  host: "localhost",
  port: 25575,
  password: "your_rcon_password"

# Fake token - Discord is not used in tests
config :nostrum,
  token: "fake_token_for_tests"
